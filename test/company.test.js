const Company = artifacts.require('../contracts/Company.sol')

require('chai').use(require('chai-as-promised')).should()

contract('Company', (accounts) => {
    describe('deployment', async() => {
        it('deploys', async => {
            
        })
})})